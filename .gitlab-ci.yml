image: gradle:6.8.3-jdk11

stages:
  - build
  - sonarqube
  - test

cache:
  paths:
    - .gradle/wrapper
    - .gradle/caches
    - build

build:
  stage: build
  image: gradle:6.8.3-jdk11
  script: 
    - gradle assemble && gradle jar
  artifacts:
    paths:
      - ./build/libs/*.jar
    expire_in: 1 week

sonarqube:
  stage: sonarqube
  script: ./gradlew sonarqube   -Dsonar.projectKey=VVPSTA   -Dsonar.host.url=http://birkenfunk.de:9000   -Dsonar.login=ecf038ba90a44902d3b419f73b5187e50a299435

test:
  stage: test
  script:
    - ls -la build/ 
    - gradle check
